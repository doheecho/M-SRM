# [개발계획서] 메-SRM 포털 및 하부 통합 모듈 코딩 최적화 (Claude 전용)

본 개발계획서는 구글 앱스 스크립트(GAS) 및 HTML5/CSS3/Vanilla JS 기반의 **'메-SRM (M-SRM) 통합 포털'**과 그 하부 모듈들의 코드를 완벽히 리팩토링하고 성능, 보안, 모바일 호환성 및 UI 대칭성을 극대화하기 위해 AI 모델(Claude)에게 최적화 작업을 지시하기 위한 가이드이자 상세 명세서입니다.

---

## 1. 프로젝트 아키텍처 및 폴더 구조 개요

메-SRM 대시보드는 스프레드시트를 백엔드 데이터베이스로 사용하며, 클라이언트단은 단일 페이지 애플리케이션(SPA) 및 모듈별 프레임 전환 방식을 채택하고 있습니다. 최근 모든 코드는 독립 파일에서 하나의 **중앙 집중형 통합 아키텍처**로 리팩토링되었으며, `"통합_"` 접두사를 가진 파일 세트로 운영됩니다.

### 1-1. 핵심 소스 파일 구성
*   **`통합_Code.gs`**
    *   **역할:** 백엔드 컨트롤러. 라우팅(`doGet`) 처리, 사용자 권한 분기, 접속로그 기록 및 외부 관계형 스프레드시트 연동(`openById`)을 총괄하는 API 허브.
    *   **통합 대상 스프레드시트:**
        *   `협력업체 관리대장` DB (ID: `1rQHrhLa3xNZ2DyfK1OS4jeF17xkfXobkV7joMoq4A4w`)
        *   `SCM Map` DB (ID: `1IcQjU0Ya21SifETdcRFFEB6mBQysXJ5MIflFsFg_hhQ`)
        *   `사용자정보/Admin` DB (ID: `1-qBeuf94mvboL0zZkfVgEAtgKsZiAAD6A6d9XjPlwb8`)
*   **`통합_index.html`**
    *   **역할:** 메인 포털 홈 화면. 바로가기 메뉴 렌더링, 실시간 RSS Live 뉴스 수집 및 카테고리 필터링 대시보드 역할 수행.
*   **`통합_header.html`**
    *   **역할:** 모든 모듈에 인클루드(`<?!= include('통합_header'); ?>`)되는 글로벌 공용 헤더. 접속자 소속/이름(예: `"구매그룹 조도희 님"`) 및 관리자 권한 배지 자동 표출, 접속로그 자동 기록 모듈 연동.
*   **`통합_module_partner.html`**
    *   **역할:** '구매 협력업체 관리대장' 기능 담당 모듈. "당사 Vendor 조회" 및 "IPC Vendor 조회" 이원화 탭 전환, 90vw 와이드형 상세 팝업창 연동.
*   **`통합_module_scm.html`**
    *   **역할:** 'SCM Map' 기능 담당 모듈. 공급망 필터링, Leaflet 지도 및 와이드형 미니맵 연동, 주소 정보 동적 노출 및 자재 세부 마일스톤 팝업 연동.

---

## 2. 모듈별 데이터베이스 스키마 및 핵심 요건

### 2-1. 메-SRM 메인 포털 (M-SRM Portal)
*   **접속로그 시트:** A(접속시간), B(접속자 구글ID), C(접속모듈명) 기록.
*   **사용자정보 시트:** A(구글 ID), B(성명), C(부서명), D(보안조회 권한: `Y` 표기).
    *   *보안 정책:* 구글 보안 강화 및 브라우저 멀티 로그인 이슈 대응을 위해 `getActiveUser().getEmail()` 실패 시 `getEffectiveUser().getEmail()`을 시도하는 강력한 Fallback 맵을 내장해야 함.
*   **바로가기 시트:** A(그룹번호), B(모듈명), C(URL), D(bi 아이콘명), E(비활성화 여부: `Y`).
*   **뉴스 및 뉴스보관 시트:** 구글 RSS에서 실시간 국내/해외 협력사 기사를 수집 및 보관.

### 2-2. 구매 협력업체 관리대장 (Partner Management)
*   **구조:** 3, 4행 병합 헤더 사양. B열부터 CS열까지 총 96개 컬럼의 방대한 데이터셋.
*   **당사 Vendor vs IPC Vendor 이원화 탭 전환:**
    *   **당사 Vendor 모드:** 전통적인 96개 컬럼 헤더 구조 보존 (A1:E1 거래정보, F1:Z1 기본정보 등).
    *   **IPC Vendor 모드:** 상장 여부(Q열) 등 일부 민감 열을 배제하고, 공장 1~4 주소(CU~DD)를 압축 결합한 45열 구조의 초정밀 그리드 적용.
*   **상세 정보 뷰어 모달 (#partnerDetailModal):**
    *   협력사 행 더블클릭 시 74vw 너비의 3분할 비대칭 레이아웃 상세 창 활성화.
    *   좌측 54% 영역에 상세 데이터 표출, 우측 46% 영역에 본사/공장 위성 지도(Leaflet) 및 20개년도 구매 실적 추이 그래프(Chart.js), 4대 재무제표(매출, 영업이익, 당기순이익, 이익잉여금) 추이 분석 시각화.

### 2-3. SCM Map (Supply Chain Map)
*   **SCM Raw 시트:** A(#), B(Vendor Code), C(Site명), D(Maker), G(협력사), H(국가), I(도시), J(위도), K(경도), L(품목), O(Risk테마), P(주소), S~Z(공정구분).
*   **동적 지도 및 미니맵 인터랙션:**
    *   **메인 지도:** 역할별 미려한 마커(divIcon) 렌더링.
    *   **축소 미니맵 (260px 와이드 Google 위성 Hybrid 레이어):** 리스트에 표시된 모든 업체의 위치를 미세한 **흰색 점(White CircleMarker)**으로 상시 표출. 메인 지도 뷰포트를 가리키는 황색 슬림 사각형(`weight: 1.2`) 렌더링.
    *   **클릭 피드백:** 리스트 행 클릭 시 미니맵 내 해당 지점에 **노란색 대형 하이라이트 동그라미**를 타겟팅하며 하단에 `"주소 : [P열 주소]"` 동적 노출.
    *   **더블클릭 피드백:** 행 더블클릭 시 세부현황 모달을 띄우고 모달 타이틀 아래에 `"주소 : [P열 주소]"` 검정색 굵은 폰트 표출.

### 2-4. 생산실적 및 계획 (Production Management)
*   **특징:** 구글 스프레드시트의 용량 한계 극복을 위해 Raw 시트에서 PIVOT 가공을 완료한 데이터셋(`실적PIVOT`, `경영계획PIVOT`, `실행계획PIVOT`)을 인출하여 차트 및 대형 대치 구조 그리드 렌더링.
*   **기능:** 파생모델/Basic모델 기준 필터링, 년도별(2020~2026) 동적 계획 수량 조회 및 수축/팽창형 Accordion UI 구조.

### 2-5. 구매 Spend 분석 (Spend Analysis)
*   **특징:** 다차원 구매실적 필터링. 캐시 미스 시 자동 AI 시사점 재생성 파이프라인 탑재.
*   **캐시 버스팅:** `CACHE_BASE_KEY`를 `SPEND_DATA_DASHBOARD_V6` 등으로 범핑하여 Stale Cache 차단. `generateInsightsAndSave()` 연동 오류 복구 완료 상태 유지 필수.

### 2-6. 재고실적 분석 (Inventory Analysis)
*   **특징:** 재고 Aging 구간별(90일 초과 부진, 180일 초과 악성재고) 금액/수량 분석 및 파트별 맞춤형 AI 시사점 추출.
*   **이메일 발송 시스템:** 이메일 개별/일괄 발송 시 본문 내 요약 표의 품목 수를 기존 3개에서 10개로 확장하며, 표의 컬럼 순서를 `Material` ➡️ `Description` ➡️ `Vendor` ➡️ `재고금액(원)` ➡️ `재고수량(EA)`으로 5열 전면 개편 완료.

---

## 3. 로컬 프론트엔드 UI/UX 개발 컨벤션 (Conventions)

Claude는 코드 리팩토링 및 신규 스타일 적용 시 다음의 로컬 디자인 및 최적화 표준 규격을 **반드시, 한 치의 오차도 없이 계승**해야 합니다.

1.  **순백색 초고속 크리티컬 패스 (Critical CSS):**
    *   페이지 첫 진입 시 리소스 다운로드 대기(White Flash)를 종식하기 위해 `<head>` 내 최상단에 순백색 가림막 배경과 최상위 적층 지수(`z-index: 999999`), 그리고 파란색 회전 로더(`.pure-spinner`) 스타일을 인라인으로 선언해야 함.
    *   외부 CDN 리소스(Bootstrap, Google Fonts, Leaflet 등)는 모두 비동기식(`media="print" onload="this.media='all'"`)으로 로드하여 렌더링 블로킹을 해제해야 함.
2.  **비동기 데이터 로딩 파이프라인 (백그라운드 SPA 적재):**
    *   `doGet()`은 즉시 빈 템플릿(데이터=null)을 초고속(50ms 미만) 반환하고, `DOMContentLoaded` 시점에 `google.script.run` 비동기 파이프라인으로 백엔드 데이터를 병렬 적재하여 점진적 화면 페인트 구현.
3.  **카드형 세그먼트 컨트롤 (Segmented Control) UI 표준:**
    *   부트스트랩의 투박한 아웃라인 버튼 그룹을 지양하고, **세그먼트 컨트롤** 레이아웃을 사용함.
    *   *비활성 버튼:* 순백색 배경(`#ffffff`), 글자 연한 회색.
    *   *컨테이너:* 둥근 형태의 부드러운 회색 배경(`#f0f1f3`), 외부 테두리 경계선 전면 제거(`border: none`), 버튼 하단 가벼운 섀도우(`box-shadow: 0 1px 2px rgba(0,0,0,0.04)`) 적용.
    *   *활성(Active) 버튼:* 삼성 블루 테마 컬러(`--samsung-blue`, `#1428A0`)로 배경을 채우고, 텍스트는 순백색 볼드체로 하이라이트.
    *   *치수 정렬:* 제어판 내 드롭다운 셀렉터, 검색창, 세그먼트 컨트롤, 제어 버튼들의 세로 높이를 정확히 **`29px`**로 수평축 상에 칼정렬함.
4.  **모바일 최적화 및 씹힘(Unresponsive) 방지:**
    *   모바일 가상 키보드 실행 시 화면 찌그러짐 및 한글 조합(Composition) 방지를 위해, 숫자/날짜 필드는 `<input type="tel">` 및 인라인 실시간 마스킹 헬퍼를 결합함.
    *   터치 이벤트 엇갈림을 방지하기 위해 중요 트리거에는 `e.stopPropagation()`을 주입하고, 모바일 친화적 인라인 `onclick` 바인딩을 적극 활용함.
5.  **파란색 볼드체 강조 표준:**
    *   리포트 테이블 내 주요 지표(예: 'Set 절감액', '연간효과', '기여효과' 등)는 통일감과 가독성을 위해 파란색 볼드체 (`color: #1428A0 !important; font-weight: bold !important;`) 스타일을 상시 부여함.

---

## 4. Claude에게 요청할 핵심 코딩 최적화 미션 (Refactoring Targets)

### 4-1. 백엔드 (Google Apps Script) 최적화
*   **Batch Read 및 CacheService 극대화:**
    *   스프레드시트에 접근하는 횟수(`SpreadsheetApp.openById`, `getSheetByName`, `getValues`)를 원천적으로 최소화하고, 단 한 번의 호출로 메모리에 로드하여 JS Object로 매핑하는 구조 고도화.
    *   `CacheService`를 설계하여 동일 데이터 쿼리 시 0ms 응답을 보장하며, 데이터 수정/삭제 발생 시에만 부분 캐시를 갱신(Cache Busting)하는 미세 관리 체계 도입.
*   **메모리 상의 해시 맵(Hash Map) 조회 구조 도입:**
    *   대용량 데이터 매칭 및 조인(Join) 연산 시, 이중 루프 `$O(N^2)$` 연산을 배제하고 고유 ID를 키로 하는 단일 `Map` 객체를 빌딩하여 조회 성능을 `$O(1)$` 또는 `$O(N)$`으로 최적화.

### 4-2. 프론트엔드 (JavaScript & DOM) 성능 최적화
*   **DOM Fragment 및 가상화 렌더링 검토:**
    *   44열 이상의 대형 테이블 렌더링 시, DOM 객체를 반복 삽입하는 대신 `DocumentFragment`를 구성하여 단 한 번의 리플로우(Reflow)로 그릴 것.
    *   필요 시 브라우저 뷰포트 내 영역만 동적으로 그리는 가상 스크롤(Virtual Scroll) 구조의 가벼운 구현을 검토할 것.
*   **이벤트 위임(Event Delegation) 및 클로저 메모리 누수 차단:**
    *   각 행마다 개별 리스너를 바인딩하지 말고, 테이블 컨테이너 수준에서 이벤트를 하이재킹하여 처리하는 구조로 일체화할 것.
    *   전역 범위 변수 및 캐시 객체(`window.ipcVendorsData` 등)의 생명주기를 정밀 제어하여 메모리 누수를 원천 방지할 것.
*   **정규식 분석 속도 최적화:**
    *   뉴스 제목 분석 시 반복 실행되는 성격 분류 정규식(RegExp)을 캐싱하여, 컴파일 비용을 최소화하고 루프 처리 속도를 가속화할 것.

### 4-3. 아키텍처 및 유지보수성 최적화
*   **공통 헬퍼 함수의 중앙 집중형 모듈화:**
    *   모듈 파일마다 개별 선언된 통화 변환(`formatCurrency`), 날짜 가공(`formatDateString`), 적용모델 포맷팅(`formatAppModels`) 등 중복 헬퍼 코드를 단일 공용 함수로 격상하고 파라미터 분기 구조로 최적화할 것.
*   **견고한 전역 에러 가드 (Error Boundary) 수립:**
    *   `try-catch` 가드를 모든 핵심 비동기 함수와 DOM 조작 지점에 적용하여, 특정 모듈이나 셀 데이터 손상 시에도 전체 웹앱 및 화면 렌더링이 멈추지 않고(무한 로딩 현상 완전 차단) 친절한 Fallback 안내가 표출되도록 설계할 것.
